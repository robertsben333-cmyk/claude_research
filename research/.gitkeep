# The research archive lives here, one directory per run day:
#
#   research/<YYYY>/<MM>/<YYYY-MM-DD>/
#
# Written by the pipeline stages, never by hand. See CLAUDE.md for the file layout
# and scripts/run_paths.py for path resolution.
