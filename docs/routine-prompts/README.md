# Moved to `edge/routine-prompts/`

The stage E Routine prompts live in [`edge/routine-prompts/`](../../edge/routine-prompts/)
since 2026-09-10, when everything about stage E was grouped into `edge/`.

- `edge-hunt.md` — the text pasted into `trig_01CvGQJWoKeNLXWCxiffM3ED` (stage E)
- `edge-execute.md` — the second exit Routine, `trig_01MPuhVvtDgvUYzZXkKpHpKD` ("Close AMC")

This stub exists because both live Routines hold prompts, pasted by hand, that refer to
`docs/routine-prompts/edge-hunt.md`, and neither prompt can be edited from an agent
session — `update_trigger` refuses any Routine an agent did not create. Delete this file
once both have been re-pasted with the new paths.
